import Vue from "vue"
import Router from "vue-router"

// import MapView from "../components/pages/MapView"

Vue.use(Router)

const router = new Router({
    routes: [
        // { path: "/", redirect: { name: "MapView" } },
        // { path: "/map", name: "MapView", component: MapView }
    ]
})

export default router
