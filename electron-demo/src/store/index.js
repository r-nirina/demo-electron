import Vue from "vue"
import Vuex, { Store } from "vuex"

// import PlayerStore from "./PlayerStore"

Vue.use(Vuex)

export default new Store({
    modules: {
        // player: PlayerStore,
    }
})
